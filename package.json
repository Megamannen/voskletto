{
  "name": "voskletto",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.ts && tsc -p tsconfig.types.json && cp build/types/voskletto.d.ts build/types/voskletto-async.d.ts .",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "esbuild": "^0.28.2",
    "playwright-core": "^1.63.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
